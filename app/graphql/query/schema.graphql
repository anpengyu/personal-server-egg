
type Query {
  
  user(id: ID!): User
  tags: [String!]
  article(id:ID!):Article
  allArticles:[Article!]
  comment(articleId:ID!):[Comment!]

  # 获取文章分类
  loadClassify:[Classify]
  loadClassifyForUser(userId:ID!):[Classify]

  #登录
  login(username:String!,password:String!):User 
  logout:User
  
  # 测试用例
  hello:String
  example_user(id:String!):Example_User
}