# 架构，定义GraphQL API的轮廓和规范

type Article {
  id: ID!
  userId: ID!
  articleTitle:String!
  articleContent:String!
  articlePraiseCount:Int!
  articleDisLikeCount:Int!
  articlePageView:Int!
  articleCommentCount:Int!
  user:User!
  updated_at: Date
  createDate: Date
  articleSubTitle:String!
  comment:[Comment!]
  label:String
  course:String
  classify:Classify
  audit:Int
  auditCause:String
}